#version 450 core

layout (
    local_size_x = 32,
    local_size_y = 1,
    local_size_z = 1
) in;

#include "storage_ssbo.glsl"

void main()
{
    uint idx = gl_GlobalInvocationID.x;
    ssOut.pos[idx] = ssIn.pos[idx] + ssIn.vel[idx];
    ssOut.vel[idx] = 0.99*ssIn.vel[idx];
}
